# sudoku_wasm
A sudoku resolver has a JS class with 2 ways of resolving :
- Entering a grid manually
- Sending a json file containing multiple grids

The site can be found in the site directory. The entry file of the WASM is in src/lib.rs.
