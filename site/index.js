// We import the sudoku class to be able to instenciate it
import { Sudoku } from './class/sudoku';

// An array that will contain all the sudokus objects
let grids = [];

// We add a first grid
grids.push(new Sudoku());